{
  "name": "semantic-release-monorepo",
  "version": "8.0.5",
  "description": "Plugins for `semantic-release` allowing it to be used with a monorepo.",
  "main": "src/index.js",
  "type": "module",
  "files": [
    "src"
  ],
  "scripts": {
    "format": "prettier --write --single-quote --trailing-comma es5",
    "format:all": "yarn format \"./**/*.js\"",
    "test": "vitest run --no-threads"
  },
  "license": "MIT",
  "peerDependencies": {
    "semantic-release": "~20.0.0"
  },
  "dependencies": {
    "debug": "^4.3.4",
    "execa": "^5.1.1",
    "file-url": "^3.0.0",
    "fs-extra": "^10.0.1",
    "get-stream": "^6.0.1",
    "git-log-parser": "^1.2.0",
    "p-each-series": "^2.1.0",
    "p-limit": "^3.1.0",
    "pkg-up": "^3.1.0",
    "ramda": "^0.27.2",
    "read-pkg": "^6.0.0",
    "semantic-release-plugin-decorators": "github:mjsorribas/semantic-release-plugin-decorators",
    "tempy": "2.0.0"
  },
  "devDependencies": {
    "husky": "^9.1.4",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.3",
    "vitest": "0.34.6"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "yarn format"
    ]
  }
}
